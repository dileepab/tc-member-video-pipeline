# Reviewer Risk Log

| Risk ID | Reviewer Could Say | Why They Might Say It | Spec Support | Current Reality | Action to Close | Appeal Strength if Deducted | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RR1 | Demo does not use a real human video. | The reviewer may only inspect the default local demo path. | Medium | The default local demo now prefers the bundled real sample clip, and the pipeline still falls back to a generated reviewer-safe sample if that file is absent. | Keep both bundled real-sample evidence and the generated fallback path documented. | Medium | Mitigated |
| RR2 | Background cleanup is not semantic segmentation. | FFmpeg focus treatment blurs the background layer but does not cut out the person. | Medium | Requirement says optional cleanup or blurring; blur is implemented. | Describe as blur/focus enhancement, not full segmentation. | Strong | Mitigated |
| RR3 | Captions are fallback-generated without OpenAI key. | Reviewers may not set paid API keys. | Medium | OpenAI adapter is included; fallback keeps offline demo reproducible. | Manifest records selected transcription adapter. | Strong | Mitigated |
| RR4 | Deployed URL missing. | Challenge asks for deployed URL. | Strong | Docker, `render.yaml`, `/health`, `/jobs`, `/render`, and `/docs` are ready, but this environment cannot create an external hosted URL without cloud account credentials. | Attach the actual hosted API URL at submission time if credentials become available; otherwise keep Docker/local verification prominent. | Weak | Open |
| RR5 | FFmpeg filters may fail on hosts without fonts/libass. | Captions and drawtext need FFmpeg compiled with libass/font support. | Medium | Dockerfile installs FFmpeg and DejaVu fonts. | Verify locally and document dependency. | Medium | Mitigated |
| RR6 | Demo video deliverable is missing. | The challenge explicitly asks for a demo video. | Strong | A walkthrough video artifact is now generated locally as `demo-output/run_app_demo.mp4`. | Keep the artifact in the final evidence bundle. | Strong | Mitigated |
| RR7 | Budget analysis ignores forum cost target. | Forum explicitly says cost should probably be below `$1` for 30 seconds. | Strong | Budget doc now shows about `$0.0047` direct variable cost per 30-second clip. | Keep the forum target called out explicitly in the budget report. | Strong | Mitigated |
| RR8 | Generated sample input may be treated as off-spec. | Reviewer may expect only real clips. | Strong | Forum explicitly allows participants to create or generate raw videos. | Keep the forum clarification logged and make generated-vs-real demo roles explicit in docs. | Strong | Mitigated |
| RR9 | Video duration or file size ignores forum limits. | Forum clarified 15-30 seconds and below 30 MB. | Strong | Pipeline now rejects materially off-window inputs, records output sizes, and rejects MP4 outputs above 30 MB. | Keep tests and manifest evidence in the final package. | Strong | Mitigated |
