# Verification Evidence Plan

| Requirement | What Must Be Shown | Source Command / Action | Output File | Captured | Notes |
| --- | --- | --- | --- | --- | --- |
| Before clip | Raw generated 18-second input exists | `python scripts/generate_sample.py` | `demo-output/before_raw_intro.mp4` | Yes | Reproducible reviewer input. |
| After desktop render | Branded landscape output with overlays/captions/audio | `python scripts/run_demo.py` | `demo-output/after/profile_landscape.mp4` | Yes | Render completed locally from the bundled 29-second sample. |
| After mobile render | Branded vertical output | `python scripts/run_demo.py` | `demo-output/after/profile_vertical.mp4` | Yes | Render completed locally from the bundled 29-second sample. |
| Captions | SRT and ASS generated | `python scripts/run_demo.py` | `demo-output/after/captions.srt`, `captions.ass` | Yes | Live OpenAI `gpt-4o-transcribe-diarize` path verified — see `submission-evidence/raw/openai-run-manifest.json` and `openai-run-captions.srt` for synced/diarized output from the real audio. |
| Tests | Unit/API smoke tests pass | `.venv/bin/python -m pytest` | `submission-evidence/raw/tests.log` | Yes | 23 tests passed on Python 3.14.3. |
| Forum output limits | Duration, dimensions, codec, and file sizes are within clarified limits | `ffprobe`, `du -h`, `manifest.json` | `demo-output/after/manifest.json` | Yes | 29-second H.264 MP4 outputs: 1280x720 at about 5 MB and 1080x1920 at about 7 MB. |
| API run | `/jobs` accepts local/S3-style job request | `.venv/bin/uvicorn app.main:app --port 8001` plus curl | `submission-evidence/raw/api-job-response.json`, `api-job-final.json` | Yes | Verified locally with generated sample source. |
| Deployment smoke (local Docker image) | Hosted-style API root, health, docs, upload render, and output download work | `PORT=8014 ... uvicorn` plus `scripts/smoke_deployment.py http://127.0.0.1:8014` | `submission-evidence/raw/deployment-smoke.txt` | Yes | Verified root/docs/health, multipart upload render, and `/outputs/...` MP4 download locally on the same Docker image used for Render. |
| Deployment smoke (live URL) | Live deployed URL serves index/health/docs | `scripts/smoke_deployment.py https://topcoder-profile-video-pipeline.onrender.com --skip-render` | `submission-evidence/raw/deployment-smoke-live.txt` | Yes | Captured 2026-04-26. Free-tier render is intentionally not exercised because 512 MB may OOM on FFmpeg; full render is covered by the local Docker smoke above. |
| Topcoder Star demo | Before/after transformation is shown in one reviewer-friendly video | `python scripts/create_before_after_demo.py` | `demo-output/topcoder_star_before_after.mp4` | Yes | Side-by-side raw clip and polished output. |
| Demo video | Walkthrough video showing how to run the app exists | Screen-recorded per `docs/WALKTHROUGH_RECORDING.md` | [Google Drive link](https://drive.google.com/file/d/1bL3winhCKszunqS0fq0QmXNUKLGIqGOx/view?usp=sharing) | Yes | Real screen recording of the full pipeline run, hosted on Google Drive (linked from `SUBMISSION.md` and `README.md`). The previous `run_app_demo.mp4` slide-only artifact is intentionally omitted from the final ZIP. |
| Custom-input render | Pipeline accepts an arbitrary 15-30 s clip with custom output paths | `python scripts/run_demo.py --input path/to/clip.mp4 --output-dir demo-output/custom-after --work-dir demo-output/custom-work` | Reviewer-generated `manifest.json` | Reviewer-runnable | Optional supplemental flow; the primary reviewer path uses the bundled sample at `demo-output/Profile_Intro_Video_Generated.mp4`. |
