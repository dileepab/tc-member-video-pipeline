# Music Assets

The demo generates a synthetic music bed with FFmpeg so the repository does not ship licensed music.

For production, place licensed WAV/MP3 loops here or pass `music_path` in `render_options`. The pipeline will loop the track and duck it under the member's voice.
